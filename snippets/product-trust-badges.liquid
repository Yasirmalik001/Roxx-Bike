<div class="product-trust-badges-bar" {{ block.shopify_attributes }}>
  <div class="trust-badge-item">
    <div class="trust-badge-icon">
      <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
        <rect x="1" y="3" width="15" height="13"></rect>
        <polygon points="16 8 20 8 23 11 23 16 16 16 16 8"></polygon>
        <circle cx="5.5" cy="18.5" r="2.5"></circle>
        <circle cx="18.5" cy="18.5" r="2.5"></circle>
      </svg>
    </div>
    <span class="trust-badge-text">{{ block.settings.text_1 | escape }}</span>
  </div>
  
  <div class="trust-badge-item">
    <div class="trust-badge-icon">
      <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
      </svg>
    </div>
    <span class="trust-badge-text">{{ block.settings.text_2 | escape }}</span>
  </div>
  
  <div class="trust-badge-item">
    <div class="trust-badge-icon">
      <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
        <path d="M21.5 2v6h-6M21.34 15.57a10 10 0 1 1-.57-8.38l5.67-5.67"></path>
      </svg>
    </div>
    <span class="trust-badge-text">{{ block.settings.text_3 | escape }}</span>
  </div>
</div>

<style>
  .product-trust-badges-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2rem 0;
    border-top: 1px solid #eeeeee;
    border-bottom: 1px solid #eeeeee;
    margin: 3rem 0;
    gap: 1.5rem;
  }
  .trust-badge-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    flex: 1;
    gap: 0.8rem;
  }
  .trust-badge-icon {
    color: rgba(var(--color-foreground), 0.7);
    background-color: #f7f7f7;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .trust-badge-text {
    font-size: 1.1rem;
    font-weight: 700;
    color: #444;
    line-height: 1.3;
    max-width: 140px;
  }
  @media screen and (max-width: 749px) {
    .product-trust-badges-bar {
      flex-direction: column;
      gap: 2rem;
      align-items: flex-start;
      padding: 2rem;
    }
    .trust-badge-item {
      flex-direction: row;
      text-align: left;
      width: 100%;
    }
    .trust-badge-text {
      max-width: 100%;
    }
  }
</style>
